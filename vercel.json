{
  "version": 2,
  "buildCommand": "cd frontend-host && npm install && npm run build",
  "outputDirectory": "frontend-host/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
