{
  "version": 2,
  "buildCommand": "npm --prefix frontend-host install && npm --prefix backend install && npm --prefix frontend-host run build",
  "outputDirectory": "frontend-host/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
