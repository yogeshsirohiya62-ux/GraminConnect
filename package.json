{
  "name": "graminconnect-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend-host && npm install && npm run build",
    "start": "cd frontend-host && npm start"
  }
}
